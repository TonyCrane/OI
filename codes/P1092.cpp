#include<bits/stdc++.h>
using namespace std;

int n, a[27];
char st[4][27];

void dfs()
{
	
}

int main()
{
	freopen("P1092.in", "r", stdin);
	scanf("%d", &n);
	for (int i = 1; i <= 3; ++i)
		scanf("%s", st[i] + 1);
	memset(a, -1, sizeof(a));
	dfs(       );
	return 0;
}
